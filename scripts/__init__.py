"""Operational scripts (data sync, training, menu) and shared helpers.

Marked as a package so first-party imports like
``from scripts.team_real_data import get_world_ranking`` resolve both from the
repo root and from an installed wheel (``pip install .``).
"""
