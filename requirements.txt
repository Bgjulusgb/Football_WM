# ── WM 2026 workflow — CORE runtime ───────────────────────────────────────────
# Enough to run a full prediction in MOCK mode (offline, no API keys):
#
#   pip install -r requirements.txt
#   python -m wm2026.cli predict --match config/matches/group_a/cze_vs_rsa.yaml
#
# Optional add-ons (charts, exact GLM, calibration, sentiment, persistent cache)
# live in requirements-optional.txt — see also the [extras] in pyproject.toml.
pydantic>=2.7,<3
pydantic-settings>=2.4
PyYAML>=6.0
httpx>=0.27
structlog>=24.1
# numpy upper-bound on 2.3 keeps us compatible with libraries that haven't
# adopted the numpy-2.4 ABI yet (e.g. numba 0.61, older lightgbm, older torch).
# wm2026 itself works on any numpy ≥1.26 — relax it (e.g. ".<3") in a venv if
# you don't need the broader ecosystem compatibility.
numpy>=1.26,<2.3
scipy>=1.11
